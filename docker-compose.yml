services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: monad_republic
      POSTGRES_USER: monad
      POSTGRES_PASSWORD: monad
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U monad -d monad_republic"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
